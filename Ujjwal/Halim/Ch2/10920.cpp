#include <bits/stdc++.h>

using namespace std;
#ifdef LOCAL
#include "uj.h"
#endif

int main( ) {
	int n;
	long long p;
	while( scanf("%d %lld", &n, &p) ) {
		int a = (sqrt(p) - 1)/2;
		int b = 2 * a + 1;
		
		int x = a, y = a;
		int [] dx = {
		while( b!= p) {
					
			
		}
		printf("Line = %d, coloumn = %d.\n");
	}
	return 0;	
}
